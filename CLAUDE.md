# trainer-reserve

パーソナルトレーナー向け予約管理サイト。
仕様・作業スタイルの詳細は `新しいチャット用_引き継ぎ書.md` を読むこと。

---

## git の鉄則（最重要・2026-09-04制定）

このリポジトリは **M1 Mac と M4 Mac の2台** で作業してる。
過去に強制上書き（force push）で GitHub 上の9コミットが消える事故が起きてるので、
以下は例外なく守ること。

### 1. 強制上書きは禁止

`--force` / `-f` / `+main:main` での push は **絶対に使わない**。
`.claude/hooks/block-force-push.py` が機械的にブロックする。
ブロックされたら、回避策を探すんじゃなくて素直にマージすること。

### 2. 作業前に pull、作業後に push

```
git pull github main
（作業）
git push github main
```

セッション開始時に `.claude/hooks/sync-check.sh` が自動で fetch して、
遅れてるだけなら自動で取り込む。分岐してたら警告を出す。

### 3. 履歴が分岐してたらマージで合流

「GitHubのみ N件 / ローカルのみ M件」と警告が出たら、
それはもう一台のMacで作業した合図。**どちらの作業も消さずに**合流させる。

```
git fetch github
git merge github/main
git push github main
```

### 4. コミットしたら push まで完了させる

ローカルに commit だけ残してると、もう一台がその存在を知らないまま作業して
また分岐する。commit と push はセット。

---

## 事故の記録（2026-08-26）

- 8/12 `5f9f58d` の時点では2台とも同じ状態だった
- M4側でコスト集計・交通費・メモ機能を9コミット作り、GitHub に push 済みだった
- M1側は pull せず、古い `5f9f58d` から空き枠カウント修正2件を作り、
  **強制上書きで push** → GitHub 上の9コミットが消滅
- M1から見ると「最初からリポジトリに存在しない」状態に見えていた
- 2026-09-04、M4のローカルに残っていた履歴をマージして復旧（`78cdfae`）

同じことを繰り返さないために上のルールがある。
